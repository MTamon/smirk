# SMIRK requirements for CUDA 12.8 / PyTorch 2.9.1 / Python 3.11.
#
# This file is aligned with MTamon/DECA (cuda128) install_128.sh and
# MTamon/FlashAvatar (release/cuda128-fixed) install_128.sh so that SMIRK can
# coexist with DECA / FlashAvatar / FLARE in the same environment.
#
# The authoritative installation path is install_128.sh (alongside this file),
# which installs the DECA128-aligned pin set with `--no-deps` to guarantee the
# exact versions. This requirements.txt exists for reference and for simple
# `pip install -r requirements.txt` usage in already-pinned environments.
#
# NOTE: chumpy must be installed from GitHub main (numpy 2.x compatible):
#       pip install git+https://github.com/mattloper/chumpy.git
# It is intentionally NOT listed below because pip cannot express a git URL
# alongside `==` pins reliably across tooling.

# --- Core deep learning stack (DECA128 / FlashAvatar128 aligned) ---
torch==2.9.1
torchvision==0.24.1
triton==3.5.1
numpy==2.2.6
scipy==1.16.3
scikit-image==0.25.2
scikit-learn==1.7.2
opencv-python==4.12.0.88
pillow==12.0.0
ImageIO==2.37.2
tifffile==2025.10.16
tqdm==4.67.1
PyYAML==6.0.3
packaging==25.0
Cython==0.29.35
kornia==0.8.2
kornia_rs==0.1.10
fvcore==0.1.5.post20221221
iopath==0.1.10
face-alignment==1.4.1
ninja==1.13.0
yacs==0.1.8
filelock==3.20.0
fsspec==2025.10.0
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.5
sympy==1.14.0
typing_extensions==4.15.0
joblib==1.5.2
threadpoolctl==3.6.0
llvmlite==0.45.1
numba==0.62.1
lazy_loader==0.4
portalocker==3.2.0
six==1.17.0
tabulate==0.9.0
termcolor==3.2.0

# --- CUDA 12.8 runtime (cu12 wheels; match DECA128 / FlashAvatar128) ---
nvidia-cublas-cu12==12.8.4.1
nvidia-cuda-cupti-cu12==12.8.90
nvidia-cuda-nvrtc-cu12==12.8.93
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cudnn-cu12==9.10.2.21
nvidia-cufft-cu12==11.3.3.83
nvidia-cufile-cu12==1.13.1.3
nvidia-curand-cu12==10.3.9.90
nvidia-cusolver-cu12==11.7.3.90
nvidia-cusparse-cu12==12.5.8.93
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.27.5
nvidia-nvjitlink-cu12==12.8.93
nvidia-nvshmem-cu12==3.3.20
nvidia-nvtx-cu12==12.8.90

# --- SMIRK-specific dependencies ---
# timm provides the tf_mobilenetv3_{small,large}_minimal_100 backbones used by
# SmirkEncoder. 0.9.16 matches the released SMIRK checkpoints' expected layer
# naming; newer timm versions may require state_dict key remapping.
timm==0.9.16
# mediapipe is used for face landmark detection in demo scripts.
# 0.10.14 is the minimum version with Python 3.11 + numpy 2.x wheels.
mediapipe==0.10.14
# albumentations 1.4.18 is the minimum version compatible with numpy 2.x.
albumentations==1.4.18
omegaconf==2.3.0
# pytorch_lightning is only needed for training; inference (demo.py,
# demo_video.py, demo_save_flame.py) works without it.
pytorch_lightning==2.5.2
gdown==5.2.0
